import type { PlayerId } from "@bloxd"
import { debugLog } from "./debugLog"

onPlayerJoin = (player: PlayerId) => {
	api.log(`Player ${player} joined - Love from Debug Plugin!`)
}

export { debugLog }
