import { handleTimers } from "./timers"

tick = () => {
	handleTimers()
}

export { setTimeout, setInterval, clearTimeout, clearInterval } from "./timers"
export type { TimeoutId, IntervalId } from "./timers"
export { forceLoadChunk } from "./chunk"
export { randomInt, shuffleArray, getRandomItem } from "./random"
